% Base de conocimiento de ejemplo para `--setup-file`.
% Aristas de un grafo, cargadas como asserts (edge/2 es :- dynamic en dynamic.pl).
% Un goal por línea; el punto final es opcional.

assertz(edge(c, d)).
assertz(edge(d, e)).
